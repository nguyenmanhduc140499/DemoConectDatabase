using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace DemoConectDatabase.Models
{
    public class AutoGeneredKey
    {
        public string GeneredKey(string id)
        {
            string strkey = "";
            //kiểm tra giá trị ID truyền vào có rỗng hay không
            //nếu ID không null 
            //tách phần chữ và phần số của ID
            //Giwuc nguyên phần chữ
            //Phần số chuyển sang kiểu số nguyên và tăng lê 1 đv
            //Ghép phần số vs phần chữ để đc mã tự sinh
            //Trả về mã sau khi tự sinh
            return strkey;
        }
    }
}